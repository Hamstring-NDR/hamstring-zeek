services:
  zeek1:
    build:
      context: ./
      dockerfile: src/Dockerfile
    cap_add:
      - NET_ADMIN
    network_mode: host
    volumes:
      - ./src:/opt/src
      - ./config.yaml:/opt/config.yaml
      - ./data/test_pcaps/:/opt/static_files