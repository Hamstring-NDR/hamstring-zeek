name: build_test_linux
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  build_test_linux:
    name: C++ Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ "ubuntu-latest" ]

    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake zip unzip tar curl pkg-config

      - name: Setup vcpkg and build
        env:
          VCPKG_ROOT: ${{ github.workspace }}/vcpkg
        run: |
          git clone https://github.com/microsoft/vcpkg.git $VCPKG_ROOT
          $VCPKG_ROOT/bootstrap-vcpkg.sh -disableMetrics
          cmake -B build -S . -DCMAKE_TOOLCHAIN_FILE=$VCPKG_ROOT/scripts/buildsystems/vcpkg.cmake -DCMAKE_BUILD_TYPE=Release
          cmake --build build -j 2

      - name: Test
        run: |
          cd build
          ctest --output-on-failure
